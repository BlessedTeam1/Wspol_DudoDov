<Window x:Class="PresentationView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Symulacja Bilarda" Height="500" Width="850">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
            <Label Content="Liczba kul:" VerticalAlignment="Center"/>

            <TextBox Text="{Binding BallCount, Mode=TwoWay}" Width="50" Margin="5,0,15,0" VerticalAlignment="Center" TextAlignment="Center"/>

            <Button Content="Start" Command="{Binding StartCommand}" Width="75" Margin="0,0,10,0"/>
            <Button Content="Stop" Command="{Binding StopCommand}" Width="75"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="Black" BorderThickness="2" Margin="10" 
                Width="{Binding CanvasWidth}" Height="{Binding CanvasHeight}" 
                HorizontalAlignment="Center" VerticalAlignment="Center">

            <ItemsControl ItemsSource="{Binding Balls}">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas ClipToBounds="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse Width="{Binding Diameter}" Height="{Binding Diameter}" Fill="Red" Stroke="DarkRed" StrokeThickness="1"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}"/>
                        <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>

            </ItemsControl>
        </Border>
    </Grid>
</Window>
